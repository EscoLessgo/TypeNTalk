{
  "name": "lovense-sync",
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "node backend/index.js",
    "build": "npm run build -w frontend && cd backend && npx prisma generate",
    "build:full": "npm install --legacy-peer-deps && npm run build -w frontend && cd backend && npx prisma generate",
    "install:all": "npm install",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build:frontend": "npm run build -w frontend"
  }
}